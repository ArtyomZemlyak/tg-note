# Реализация векторного поиска для автономного агента

## ✅ Выполнено

### 1. Модуль векторного поиска (`src/vector_search/`)
**Создано 6 Python файлов (2212 строк кода):**

- **embeddings.py**: Модели эмбеддингов
  - SentenceTransformerEmbedder (локальные эмбеддинги)
  - OpenAIEmbedder (OpenAI API)
  - InfinityEmbedder (Infinity API)

- **vector_stores.py**: Векторные хранилища
  - FAISSVectorStore (локальный FAISS)
  - QdrantVectorStore (Qdrant API)

- **chunking.py**: Разбиение документов
  - Стратегия FIXED_SIZE (фиксированный размер)
  - Стратегия FIXED_SIZE_WITH_OVERLAP (с перекрытием)
  - Стратегия SEMANTIC (по структуре документа)

- **manager.py**: Менеджер векторного поиска
  - Индексация базы знаний
  - Отслеживание изменений файлов
  - Инкрементальная переиндексация
  - Валидация конфигурации

- **factory.py**: Фабрика компонентов
  - Создание из настроек
  - Автоматическая инициализация

- **__init__.py**: Экспорты модуля

### 2. Настройки (`config/settings.py`)
**Добавлено 20+ параметров конфигурации:**

#### Основные настройки
- `VECTOR_SEARCH_ENABLED`: Включение/выключение (по умолчанию: false)

#### Модели эмбеддингов
- `VECTOR_EMBEDDING_PROVIDER`: Выбор провайдера (sentence_transformers/openai/infinity)
- `VECTOR_EMBEDDING_MODEL`: Название модели
- `VECTOR_INFINITY_API_URL`: URL Infinity API
- `VECTOR_INFINITY_API_KEY`: API ключ Infinity

#### Векторные хранилища
- `VECTOR_STORE_PROVIDER`: Выбор хранилища (faiss/qdrant)
- `VECTOR_QDRANT_URL`: URL Qdrant
- `VECTOR_QDRANT_API_KEY`: API ключ Qdrant
- `VECTOR_QDRANT_COLLECTION`: Имя коллекции

#### Разбиение на чанки
- `VECTOR_CHUNKING_STRATEGY`: Стратегия разбиения
- `VECTOR_CHUNK_SIZE`: Размер чанка (по умолчанию: 512)
- `VECTOR_CHUNK_OVERLAP`: Перекрытие чанков (по умолчанию: 50)
- `VECTOR_RESPECT_HEADERS`: Учитывать заголовки markdown

#### Поиск
- `VECTOR_SEARCH_TOP_K`: Количество результатов (по умолчанию: 5)

### 3. Интеграция с агентом (`src/agents/autonomous_agent.py`)
**Добавлено 2 новых тулза:**

- **kb_vector_search**: Семантический поиск по базе знаний
  ```python
  {
      "query": "neural networks",
      "top_k": 5
  }
  ```

- **kb_reindex_vector**: Переиндексация базы знаний
  ```python
  {
      "force": false  # принудительная переиндексация
  }
  ```

### 4. Зависимости (`pyproject.toml`)
**Добавлена опциональная группа `vector-search`:**
```bash
pip install -e '.[vector-search]'
```

Включает:
- `sentence-transformers>=2.2.0`
- `faiss-cpu>=1.7.4`
- `qdrant-client>=1.7.0`

### 5. Документация
- `src/vector_search/README.md`: Полная документация модуля
- `config.example.yaml`: Детальные примеры конфигурации с комментариями
- `VECTOR_SEARCH_IMPLEMENTATION.md`: Техническая документация

### 6. Примеры (`examples/vector_search_example.py`)
**Демонстрация всех возможностей:**
- Настройка с sentence-transformers + FAISS
- Настройка с OpenAI + FAISS
- Настройка с sentence-transformers + Qdrant
- Интеграция с автономным агентом
- Сравнение стратегий разбиения
- Сравнение текстового и векторного поиска

### 7. Тесты (`tests/test_vector_search.py`)
**16 тестов для проверки функциональности:**
- Разбиение фиксированного размера
- Разбиение с перекрытием
- Семантическое разбиение
- Разбиение по параграфам
- Обработка edge cases
- Сохранение метаданных
- Интеграционные тесты

## 🎯 Ключевые особенности

### 1. Гибкая конфигурация
Пользователь может выбрать:
- **Модель эмбеддингов**: 3 варианта (локальные, OpenAI, Infinity)
- **Векторное хранилище**: 2 варианта (FAISS, Qdrant)
- **Стратегию разбиения**: 3 варианта (fixed_size, overlap, semantic)

### 2. Автоматическое управление
- Отслеживание изменений файлов
- Инкрементальная индексация (только измененные файлы)
- Автоматическая переиндексация при изменении конфигурации
- Graceful degradation при отсутствии зависимостей

### 3. По умолчанию выключено
- Не требует зависимостей, если не используется
- Явное включение через конфигурацию
- Не влияет на существующую функциональность

### 4. Production-ready
- Обработка ошибок
- Логирование
- Метаданные для отслеживания
- Тесты
- Документация

## 📊 Статистика

- **Файлов создано**: 11
- **Строк кода**: 2212
- **Модулей**: 6
- **Тестов**: 16
- **Примеров**: 1 (с 6 демонстрациями)
- **Тулзов добавлено**: 2
- **Настроек добавлено**: 20+

## 🚀 Использование

### Базовая настройка
```yaml
# config.yaml
VECTOR_SEARCH_ENABLED: true
VECTOR_EMBEDDING_PROVIDER: "sentence_transformers"
VECTOR_EMBEDDING_MODEL: "all-MiniLM-L6-v2"
VECTOR_STORE_PROVIDER: "faiss"
```

### Установка зависимостей
```bash
pip install -e '.[vector-search]'
```

### Использование с агентом
```python
from src.vector_search import VectorSearchFactory

# Создание из настроек
manager = VectorSearchFactory.create_from_settings(
    settings=settings,
    kb_root_path=kb_root_path
)

await manager.initialize()
await manager.index_knowledge_base()

# Создание агента
agent = AutonomousAgent(
    enable_vector_search=True,
    vector_search_manager=manager
)
```

## ✨ Преимущества

1. **Семантический поиск**: Находит документы по смыслу, а не только по ключевым словам
2. **Гибкость**: Множество вариантов моделей и хранилищ
3. **Приватность**: Опция полностью локальной работы (sentence-transformers + FAISS)
4. **Масштабируемость**: Поддержка production-ready решений (Qdrant)
5. **Производительность**: Опциональное GPU ускорение
6. **Простота**: Автоматическая настройка из конфигурации

## 🔄 Важные моменты

### При изменении конфигурации
При изменении модели или векторного хранилища требуется полная переиндексация:
- Изменение `VECTOR_EMBEDDING_PROVIDER` или `VECTOR_EMBEDDING_MODEL`
- Изменение `VECTOR_STORE_PROVIDER`
- Изменение стратегии или параметров разбиения

Переиндексация происходит автоматически при обнаружении изменений.

### Инкрементальное обновление
При изменении файлов в базе знаний:
- Отслеживаются хеши содержимого файлов
- Индексируются только измененные файлы
- Метаданные сохраняются в `.vector_index/metadata.json`

### Производительность
- **Модели**: от 384 (быстро) до 3072 (качество) измерений
- **Чанки**: от 256 (точность) до 2048 (контекст) символов
- **GPU**: Автоматическое использование при наличии

## 📝 Итоги

Реализована полнофункциональная система векторного поиска с:
- ✅ Поддержкой 3 моделей эмбеддингов
- ✅ Поддержкой 2 векторных хранилищ
- ✅ 3 стратегиями разбиения документов
- ✅ Автоматической индексацией и переиндексацией
- ✅ Интеграцией с автономным агентом
- ✅ Полной документацией и примерами
- ✅ Тестами
- ✅ Гибкой конфигурацией

Система готова к использованию и легко расширяется для добавления новых моделей и хранилищ.
